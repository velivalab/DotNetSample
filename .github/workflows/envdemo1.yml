name: envdemo1


on:
  workflow_dispatch:


jobs:
  deploy:
    name: Deploy
    environment: Test
    runs-on: ubuntu-latest
    steps:    
      - name: Deployment 
        run: echo Started deployment.
